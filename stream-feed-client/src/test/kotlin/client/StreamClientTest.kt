package client

import org.amshove.kluent.`should be equal to`
import org.junit.jupiter.api.Test

internal class StreamClientTest {

    @Test
    fun emptyTest() {
        1 + 1 `should be equal to` 2
    }
}